export enum transactionStatus {
  PAID = "paid",
  PREPARING = "preparing",
  SERVED = "served",
}

export enum paymentMethod {
  CASH = "cash",
  QRIS = "qris",
}

export enum transactionType {
  DINE_IN = "dine in",
  TAKE_AWAY = "take_away",
}
